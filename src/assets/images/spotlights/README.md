# Spotlight photos

Add one image per spotlight, e.g. `january-2026.png`. Reference the path in `src/content/spotlights.edn` as `/assets/images/spotlights/january-2026.png`. The build copies this folder to the published site.
