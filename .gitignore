# Meta directory (client resources, not part of codebase)
meta/

# Build output
public/
dist/

# Dependencies
node_modules/
.venv/
__pycache__/

# Plugin: lockfile not needed (target/cache-images.js is checked in)
plugins/cache-images/package-lock.json
# Plugin: shadow-cljs build cache (only target/cache-images.js is committed)
plugins/cache-images/.shadow-cljs/

# Netlify functions: lockfile optional (cms-auth/, cms-callback/ built JS is checked in)
netlify/functions/package-lock.json
# Netlify functions: shadow-cljs build cache (compiled JS is committed, no Java on CI)
netlify/functions/.shadow-cljs/
# shadow-cljs build manifest (regenerated on first build; used in plugins/ and netlify/functions)
manifest.edn

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp

# Clojure/Kondo cache
.clj-kondo/
.lsp/
